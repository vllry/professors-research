apiVersion: v1
kind: Service
metadata:
  name: professors-research-web
  namespace: professors-research
  labels:
    app: professors-research-web
spec:
  type: ClusterIP
  selector:
    app: professors-research-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

